version: 1.0
provider:
  name: openfaas
  gateway: ${FAASD_GATEWAY_URL:-http://127.0.0.1:8080}
functions:
  tree-a:
    lang: node24
    handler: ./A
    image: ./dist/tree-a.tar
    platforms: linux/amd64,linux/arm64
    limits:
      cpu: ${WORKFLOW_CPU_LIMIT:-0.25}
      memory: ${WORKFLOW_MEMORY_LIMIT:-256Mi}
  tree-b:
    lang: node24
    handler: ./B
    image: ./dist/tree-b.tar
    platforms: linux/amd64,linux/arm64
    limits:
      cpu: ${WORKFLOW_CPU_LIMIT:-0.25}
      memory: ${WORKFLOW_MEMORY_LIMIT:-256Mi}
  tree-c:
    lang: node24
    handler: ./C
    image: ./dist/tree-c.tar
    platforms: linux/amd64,linux/arm64
    limits:
      cpu: ${WORKFLOW_CPU_LIMIT:-0.25}
      memory: ${WORKFLOW_MEMORY_LIMIT:-256Mi}
  tree-d:
    lang: node24
    handler: ./D
    image: ./dist/tree-d.tar
    platforms: linux/amd64,linux/arm64
    limits:
      cpu: ${WORKFLOW_CPU_LIMIT:-0.25}
      memory: ${WORKFLOW_MEMORY_LIMIT:-256Mi}
  tree-e:
    lang: node24
    handler: ./E
    image: ./dist/tree-e.tar
    platforms: linux/amd64,linux/arm64
    limits:
      cpu: ${WORKFLOW_CPU_LIMIT:-0.25}
      memory: ${WORKFLOW_MEMORY_LIMIT:-256Mi}
  tree-f:
    lang: node24
    handler: ./F
    image: ./dist/tree-f.tar
    platforms: linux/amd64,linux/arm64
    limits:
      cpu: ${WORKFLOW_CPU_LIMIT:-0.25}
      memory: ${WORKFLOW_MEMORY_LIMIT:-256Mi}
  tree-g:
    lang: node24
    handler: ./G
    image: ./dist/tree-g.tar
    platforms: linux/amd64,linux/arm64
    limits:
      cpu: ${WORKFLOW_CPU_LIMIT:-0.25}
      memory: ${WORKFLOW_MEMORY_LIMIT:-256Mi}
configuration:
  templates:
    - name: node24
      source: https://github.com/mactavishz/faas-templates